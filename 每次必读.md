# 每次必读

> 位于仓库根目录的**操作纪律**。
> **开工前先读，收工前必记。**

## 一、为什么有这个文件

出现过这样的失误：需求方已经拍板的事（例如项目命名），我在下一轮又问了一遍。
根因很简单——**跨轮次之间，我并不天然记得自己做过什么、决定了什么**。

所以：记录不是可选项，是流程的一部分。**没有记录的完成，等于没完成。**

## 二、开工前：先读这些

| 顺序 | 文件 | 作用 |
| --- | --- | --- |
| 1 | **`docs/STATUS.md`** | **当前进度的唯一权威**：已完成 / 进行中 / 下一步 / 阻塞 / 待定问题 |
| 2 | `.dsh/progress.log` | 按时间追加的流水账（本地，不入库） |
| 3 | `.dsh/todos.json` | 当前聚焦待办（本地，不入库） |

**不要凭印象判断进度。先读 `docs/STATUS.md`，再动手。**

## 三、收工前：必须更新

完成任何一件事（哪怕很小）后，按顺序执行：

1. **更新 `docs/STATUS.md`** —— 已完成 / 进行中 / 下一步 / 阻塞项，并刷新「最后更新」时间
2. **追加 `.dsh/progress.log`** —— 带时间戳的一条：做了什么、验证了什么、踩了什么坑
3. **更新 `.dsh/todos.json`** —— 勾掉已完成的，补上新识别的任务
4. **有决策就写进 ADR** —— `docs/DESIGN.md` 附录 A 的决策记录表
5. **提交** —— 提交信息遵循 Conventional Commits（`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:`）

## 四、硬性纪律

- **已定案的事不再重复询问**：决策一旦记入 `STATUS.md` 或 ADR，即视为定案
- **不确定的先查，不要猜**：仓库状态用 git 查，环境状态用命令查，不要凭记忆
- **公开仓库红线**：密钥、令牌、端点地址一律不入库；推送前必跑 `npm run check:secrets`
- **提交 ≠ 构建**：日常小步提交；macOS/Windows 构建只在里程碑打 tag 时触发
- **脚本一律用 ESM**：本仓库 `package.json` 含 `"type": "module"`，`.js` 脚本用 `require` 会静默失败
- **做完就验证**：能跑的命令就跑，用真实输出证明它工作，而不是「应该没问题」

## 五、常用检查命令

```bash
npm run typecheck      # 类型检查
npm run build          # 类型检查 + 前端构建
npm run check:secrets  # 密钥扫描（推送前必跑）
git status             # 工作区状态
```
