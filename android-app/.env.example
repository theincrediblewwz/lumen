# Optional public Gateway URL. Leave blank for personal BYOK use.
# Never place an AI provider key, WebDAV password or other secret in EXPO_PUBLIC_*.
# A non-local Gateway must use HTTPS. Keep user-specific endpoints in app settings.
EXPO_PUBLIC_AI_GATEWAY_URL=

