feat(ui): draggable AI FAB + movable & resizable node panel

Fixes two overlap/interaction complaints:
1. The AI FAB now drags anywhere (position persisted); a small movement
   threshold distinguishes drag from click, so it no longer sits on top
   of the node panel's delete button — move either one out of the way.
2. The node panel is now movable (drag its header) and resizable from
   all four edges and corners (drag an edge to grow that side). Position
   and size persist to localStorage and are clamped to the viewport.

- AiFab.tsx: pointer-drag with right/bottom anchoring + persistence
- NodePanel.tsx: floating rect state, header move handle, 8 resize
  handles, min size + viewport clamping, persistence
- styles.css: .np-resize handles, grab cursor on header, is-floating
- bump version to 0.1.4
