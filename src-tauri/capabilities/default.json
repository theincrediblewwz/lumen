{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "最小权限：仅允许调用应用自定义命令。文件/网络等能力在 M1-M5 按需显式开启。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
