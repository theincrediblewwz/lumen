{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "最小权限：自定义命令 + 目录选择对话框。文件读写走 Rust 侧 std::fs，不暴露 fs 插件给前端。",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
